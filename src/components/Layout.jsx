import React from "react";
import { Outlet } from "react-router-dom";

function Layout() {
    return(
        <main>
            <header>

            </header>
            <section>

            </section>
            <footer>

            </footer>
        </main>
    )
}

export default Layout